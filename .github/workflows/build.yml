name: build

on:
  push:
    branches: ["**"]
  pull_request:

defaults:
  run:
    working-directory: deep-shaft-tycoon

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"

      - name: Cache the Connect IQ SDK
        id: sdk-cache
        uses: actions/cache@v4
        with:
          path: deep-shaft-tycoon/build/sdk
          key: connectiq-sdk-${{ hashFiles('deep-shaft-tycoon/tools/build.sh') }}

      - name: Check the round-screen layout
        run: python3 tools/check_layout.py

      - name: Build for the Venu 2 family
        run: tools/build.sh --fetch-sdk

      - uses: actions/upload-artifact@v4
        with:
          name: deep-shaft-prg
          path: deep-shaft-tycoon/build/*.prg
          if-no-files-found: error
